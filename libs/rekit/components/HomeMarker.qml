import QtQuick

// Subtle pill hinting at the home-gesture zone (swipe up from the visual
// bottom edge / long press).
Rectangle {
    width: 220
    height: 8
    radius: 4
    color: "black"
    opacity: 0.4
}
