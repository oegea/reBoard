# reBoard store package manifest for the Terminal app. {APP_DIR} expands to
# the install directory on the device (ADR-0006).
name=Terminal
exec={APP_DIR}/reboard-terminal
icon={APP_DIR}/icon.png
dock=false
